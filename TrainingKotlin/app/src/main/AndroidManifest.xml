<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.abraham24.trainingkotlin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NewMVP.Activities.MVPFormulaActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WithoutMVP.SecondActivity" />
        <activity android:name=".MVP.MainMVPActivity" />
        <activity android:name=".WithoutMVP.HitungBangunDatar" />
        <activity android:name=".NewMVP.Activities.PersegiPanjangActivity" />
        <activity android:name=".NewMVP.Activities.SegiTigaActivity" />
        <activity android:name=".NewMVP.Activities.PersegiActivity"></activity>
    </application>

</manifest>